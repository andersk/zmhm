#!/bin/bash

configfile=/etc/zmhm.conf

set -e
. /usr/share/debconf/confmodule

if [ -e $configfile ]; then
    db_set zmhm/servers `cat /etc/zmhm.conf`
fi

db_input medium zmhm/servers || true
db_go || true
